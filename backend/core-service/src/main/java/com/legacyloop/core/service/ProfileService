package com.legacyloop.core.service;

import com.legacyloop.common.dto.PageResponse;
import com.legacyloop.core.dto.request.UpdateAlumniProfileRequest;
import com.legacyloop.core.dto.request.UpdateStudentProfileRequest;
import com.legacyloop.core.dto.response.AlumniProfileResponse;
import com.legacyloop.core.dto.response.StudentProfileResponse;
import com.legacyloop.common.event.UserRegisteredEvent;
import org.springframework.data.domain.Pageable;

public interface ProfileService {

    /** Called by the user.registered consumer - creates the blank domain profile. */
    void createFromRegistration(UserRegisteredEvent event);

    StudentProfileResponse myStudentProfile();

    StudentProfileResponse updateStudentProfile(UpdateStudentProfileRequest request);

    AlumniProfileResponse myAlumniProfile();

    AlumniProfileResponse updateAlumniProfile(UpdateAlumniProfileRequest request);

    PageResponse<AlumniProfileResponse> mentors(Pageable pageable);
}
