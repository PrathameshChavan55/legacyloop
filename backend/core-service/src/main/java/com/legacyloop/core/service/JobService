package com.legacyloop.core.service;

import com.legacyloop.common.dto.PageResponse;
import com.legacyloop.core.dto.request.CreateJobRequest;
import com.legacyloop.core.dto.request.UpdateJobRequest;
import com.legacyloop.core.dto.response.JobResponse;
import org.springframework.data.domain.Pageable;

import java.math.BigDecimal;

public interface JobService {

    JobResponse create(CreateJobRequest request);

    JobResponse update(Long jobId, UpdateJobRequest request);

    JobResponse getById(Long jobId);

    PageResponse<JobResponse> search(String keyword, String skills, String location,
                                     String employmentType, Long companyId, BigDecimal minSalary,
                                     String status, Pageable pageable);

    PageResponse<JobResponse> myPostings(Pageable pageable);

    void close(Long jobId);
}
