package com.legacyloop.core.service;

import com.legacyloop.common.dto.PageResponse;
import com.legacyloop.core.dto.request.ApplyRequest;
import com.legacyloop.core.dto.request.GrantReferralRequest;
import com.legacyloop.core.dto.request.UpdateApplicationStatusRequest;
import com.legacyloop.core.dto.response.ApplicationResponse;
import com.legacyloop.core.dto.response.ReferralResponse;
import org.springframework.data.domain.Pageable;

public interface ApplicationService {

    ApplicationResponse apply(Long jobId, ApplyRequest request);

    ApplicationResponse getById(Long applicationId);

    PageResponse<ApplicationResponse> myApplications(Pageable pageable);

    PageResponse<ApplicationResponse> forJob(Long jobId, Pageable pageable);

    ApplicationResponse updateStatus(Long applicationId, UpdateApplicationStatusRequest request);

    ReferralResponse grantReferral(GrantReferralRequest request);

    PageResponse<ReferralResponse> myReferrals(Pageable pageable);
}
