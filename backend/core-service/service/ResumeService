package com.legacyloop.core.service;

import com.legacyloop.core.dto.response.ResumeResponse;
import org.springframework.web.multipart.MultipartFile;

import java.util.List;

public interface ResumeService {

    ResumeResponse upload(MultipartFile file, boolean makePrimary);

    List<ResumeResponse> myResumes();

    ResumeResponse setPrimary(Long resumeId);

    void delete(Long resumeId);
}
