# npm refuses an install when a package still declares React 18 as its peer. Every dependency here
# supports React 19, but a transitive one occasionally lags a release behind — this keeps the
# install from failing on a warning. Remove it if you ever want npm to be strict again.
legacy-peer-deps=true
